name: Quiz Build and Validation

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  # Job de validação básica
  validate:
    name: Validar Arquivos
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Verificar arquivos essenciais
        run: |
          echo "🔍 Verificando arquivos essenciais..."
          
          # Verificar se os arquivos existem
          if [ ! -f "index.html" ]; then
            echo "❌ index.html não encontrado"
            exit 1
          fi
          
          if [ ! -f "styles.css" ]; then
            echo "❌ styles.css não encontrado"
            exit 1
          fi
          
          if [ ! -f "script.js" ]; then
            echo "❌ script.js não encontrado"
            exit 1
          fi
          
          echo "✅ Todos os arquivos essenciais encontrados"

      - name: Verificar estrutura HTML
        run: |
          echo "🔍 Verificando estrutura HTML..."
          
          # Verificar DOCTYPE
          if ! grep -q "<!DOCTYPE html>" index.html; then
            echo "❌ DOCTYPE não encontrado"
            exit 1
          fi
          
          # Verificar links para CSS e JS
          if ! grep -q "styles.css" index.html; then
            echo "❌ Link para styles.css não encontrado"
            exit 1
          fi
          
          if ! grep -q "script.js" index.html; then
            echo "❌ Link para script.js não encontrado"
            exit 1
          fi
          
          echo "✅ Estrutura HTML válida"

      - name: Verificar estrutura JavaScript
        run: |
          echo "⚡ Verificando estrutura JavaScript..."
          
          # Verificar se tem dados do quiz
          if ! grep -q "quizData" script.js; then
            echo "❌ Dados do quiz não encontrados"
            exit 1
          fi
          
          # Verificar se tem funções principais
          if ! grep -q "loadQuestion" script.js; then
            echo "❌ Função loadQuestion não encontrada"
            exit 1
          fi
          
          echo "✅ Estrutura JavaScript válida"

  # Job de build e preparação
  build:
    name: Build e Preparação
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Preparar arquivos
        run: |
          echo "📁 Preparando arquivos para distribuição..."
          
          # Criar diretório de distribuição
          mkdir -p dist
          
          # Copiar arquivos principais
          cp index.html dist/
          cp styles.css dist/
          cp script.js dist/
          cp README.md dist/
          
          echo "✅ Arquivos copiados para dist/"

      - name: Criar arquivo de informações
        run: |
          echo "📊 Criando arquivo de informações..."
          
          cat > dist/build-info.json << EOF
          {
            "build_date": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "commit": "${{ github.sha }}",
            "branch": "${{ github.ref_name }}",
            "workflow": "${{ github.workflow }}",
            "build_number": "${{ github.run_number }}"
          }
          EOF
          
          echo "✅ Arquivo de informações criado"

      - name: Criar arquivo de instruções
        run: |
          echo "📋 Criando arquivo de instruções..."
          
          cat > dist/COMO-USAR.md << EOF
          # 🎯 Quiz de Ciências da Computação
          
          ## 📁 Arquivos Incluídos
          - \`index.html\` - Página principal do quiz
          - \`styles.css\` - Estilos CSS responsivos
          - \`script.js\` - Lógica JavaScript
          - \`README.md\` - Documentação completa
          - \`build-info.json\` - Informações do build
          
          ## 🚀 Como Usar
          1. Baixe todos os arquivos desta pasta
          2. Abra \`index.html\` em qualquer navegador
          3. Responda às perguntas do quiz
          4. Veja sua pontuação final!
          
          ## 🎮 Controles
          - Clique nas opções para responder
          - Use as teclas 1-4 para navegar pelas opções
          - Pressione Enter para avançar
          - Use "Jogar Novamente" para reiniciar
          
          ## 📊 Sobre o Quiz
          - 8 perguntas sobre Ciências da Computação
          - Interface responsiva para desktop e mobile
          - Sistema de pontuação com feedback visual
          - Temas: Programação, Algoritmos, Redes, Estruturas de Dados
          
          **Build:** v${{ github.run_number }}
          **Commit:** ${{ github.sha }}
          **Data:** $(date '+%d/%m/%Y %H:%M')
          EOF
          
          echo "✅ Arquivo de instruções criado"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: quiz-ciencias-computacao-v${{ github.run_number }}
          path: dist/
          retention-days: 30

  # Job de notificação
  notify:
    name: Notificar Sucesso
    runs-on: ubuntu-latest
    needs: [validate, build]
    if: always()
    steps:
      - name: Status Final
        run: |
          if [ "${{ needs.validate.result }}" == "success" ] && [ "${{ needs.build.result }}" == "success" ]; then
            echo "🎉 Build concluído com sucesso!"
            echo "📁 Arquivos disponíveis como artifact"
            echo "📦 Nome do artifact: quiz-ciencias-computacao-v${{ github.run_number }}"
            echo "🔗 Acesse a aba Actions para baixar"
            echo "📋 Instruções incluídas no arquivo COMO-USAR.md"
          else
            echo "❌ Build falhou"
            echo "🔍 Verifique os logs para mais detalhes"
            exit 1
          fi